set mbox_type   = Maildir

set folder      = ~/.mail/
set spoolfile   = "+INBOX"
set postponed   = "+Drafts"
# unset record
set copy=yes
set record="+Sent"

mailboxes "+INBOX" "+Drafts" "+Sent" "+Spam"

# vim: filetype=muttrc
